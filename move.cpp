/*struct bricks
{
  int brick_x;
  int brick_y;
  int brick_width;
  int brick_length;
  char *zhuan;
}brick[35];
*/


/*   begin:
       dotvy=-5;
       dotvx=0;


   if( ( dotx < 0 ) || ( dotx + dot_WIDTH > SCREEN_WIDTH ) )
    {
        //move back
        dotvy = 5;
         dotvx = 5;
        doty += dotvy;
        dotx -= dotvx;
    }
    if( ( doty< 0 ) || ( doty + dot_HEIGHT > SCREEN_HEIGHT ) )
    {
        //move back
        dotvy = 5;
        dotvx = 5;
        doty += dotvy;
        dotx -= dotvx;
    }
*/
